// Sonascope — TypeScript audio analysis library + browser workbench.

export const VERSION = '0.1.0';
export const NAME = 'sonascope';

export function describe(): string {
  return `${NAME} v${VERSION}`;
}
